.c-video-player {
  position: relative;
  width: 100%;
  max-width: calc(var(--space-char-per-line) * 1ch);
  padding-top: 66%;
}

.c-video-player > iframe {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
}
