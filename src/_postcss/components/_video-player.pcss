.c-video-player {
  width: 100%;
  max-width: calc(var(--space-char-per-line) * 1ch);
  padding-top: 66%;
  position: relative;
}

.c-video-player > iframe {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
}
