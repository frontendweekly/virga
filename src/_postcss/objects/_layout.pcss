.o-wrapper {
  --space-char-per-line: 72;

  max-width: calc(var(--space-char-per-line) * 1ch);
  margin-left: auto;
  margin-right: auto;
}

.o-inner-wrapper {
  max-width: calc(var(--space-char-per-line) * 1ch);
  margin-left: auto;
  margin-right: auto;
}
